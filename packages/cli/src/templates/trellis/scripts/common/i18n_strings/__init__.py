"""Trellis Python i18n string dictionaries.

Each locale module exposes a single ``STRINGS: dict[str, str]`` mapping where
keys follow the convention ``<file_stem>.<short_action>`` (see
``research/config-and-python-i18n.md`` §5.3).

Technical names (paths, commands, identifiers) MUST stay verbatim — only
prose-y user-facing strings get translated.
"""
