# Historical task fixture

This offline fixture contains only the requirement facts visible before implementation. It deliberately excludes implementation results, test outcomes, and completed-task answers. `requirements.json` is rendered into the paired native and reviewable documents for structural regression checks only.
